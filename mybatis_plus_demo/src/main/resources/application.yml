# mybatis config
mybatis-plus:
  type-aliases-package: com.wx.demo.entity
  mapper-locations: classpath*:mapper/*.xml
  # 这个配置会将执行的sql打印出来
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
